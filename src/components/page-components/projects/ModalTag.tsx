import { FunctionComponent } from 'react'

interface Props {
  isOpen?: boolean
  onClose?: () => void
}

const TagModal: FunctionComponent<Props> = ({ onClose, isOpen }) => {
  return (
    <div />
  )
}

export default TagModal
